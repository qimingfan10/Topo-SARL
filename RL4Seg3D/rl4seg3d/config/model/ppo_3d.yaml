defaults:
  - RLModule_3d
  - /actor: 3d_ac_unet

_target_: rl4seg3d.PPO_3d.PPO3D

clip_value: 0.2        # 允许适度的策略更新
k_steps_per_batch: 4
entropy_coeff: 0.03    # 🔧 修复：降低到合理值，避免策略完全随机化（之前0.3导致100%全屏mask）
divergence_coeff: 0.015
